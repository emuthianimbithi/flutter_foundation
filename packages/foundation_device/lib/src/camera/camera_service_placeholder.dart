class CameraServicePlaceholder {
  /// Implement with `camera` package (recommended) in your app or in a follow-up package.
  Future<void> openCamera() async {
    throw UnimplementedError('Provide a concrete camera implementation using the camera plugin.');
  }
}