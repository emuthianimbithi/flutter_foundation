class BarcodeScannerPlaceholder {
  /// Implement with `mobile_scanner` in your app.
  Future<String?> scan() async {
    throw UnimplementedError('Provide a concrete barcode scanner implementation.');
  }
}