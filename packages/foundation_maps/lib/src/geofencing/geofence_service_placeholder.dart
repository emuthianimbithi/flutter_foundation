class GeofenceServicePlaceholder {
  /// Implement with a geofencing plugin if/when needed.
  Future<void> start() async {
    throw UnimplementedError('Provide a concrete geofencing implementation.');
  }
}