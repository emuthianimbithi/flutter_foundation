# Foundation Showcase

A small demo app that wires together the Flutter Foundation packages and provides a quick way to visually test components.

## Run inside the monorepo

From repo root:

```bash
cd apps/showcase
flutter pub get
flutter run
```

## Notes
- Maps need API keys (Google/Mapbox).
- Payments require provider SDK + backend intent/order creation; the showcase demonstrates the UI + interfaces.
